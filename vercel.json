{
  "version": 2
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/movies/(?<slug>.*)", "dest": "/api/movies/[slug]" },
    { "src": "/series/(?<slug>.*)", "dest": "/api/series/[slug]" },
    { "src": "/", "dest": "/api/index" }
  ]
}
